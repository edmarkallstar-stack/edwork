rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TODO: scope by branchId, userId, type
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
